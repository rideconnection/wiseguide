<%= render 'shared/breadcrumbs', :target => @trip_authorization, :name => 'Trip Authorization' %>
<%= link_to "Delete", trip_authorization_path(@trip_authorization), :method => "delete", :confirm => "Are you sure you want to delete this trip authorization?", :class => "delete" if can?(:delete, @trip_authorization) %>

<%= render 'form' %>

<%= last_updated @trip_authorization %><br>