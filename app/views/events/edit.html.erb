<%= render 'shared/breadcrumbs', :target => @event, :name => 'Event' %>

<%= link_to "Delete", event_path(@event), :method => "delete", :confirm => "Are you sure you want to delete this event?", :class => "delete" if can?(:delete, @event) %>

<%= render "form" %>

<%= last_updated @event %><br/>
