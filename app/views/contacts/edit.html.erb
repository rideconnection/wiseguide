<%= render 'shared/breadcrumbs', target: @contactable, name: "#{@contactable.class.humanized_name} Contact" %>
<%= link_to "Delete", contact_path(@contact), method: "delete", data: {confirm: "Are you sure you want to delete this contact?"}, class: "delete" if can?(:delete, @contact) %>

<%= render "form" %>

<%= last_updated @contact %>
